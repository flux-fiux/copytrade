import { TerminalLayout } from "@/components/terminal/terminal-layout";

export const metadata = { title: "Terminal — ArbMind" };

export default function TerminalPage() {
  return <TerminalLayout />;
}
